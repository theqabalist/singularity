machine:
    node:
        version: 0.11.13
dependencies:
    pre:
        - sudo apt-get update; sudo apt-get install -y jshon
test:
    post:
        - node_modules/.bin/gulp style
        - node_modules/.bin/jasmine-node spec
        - ./package