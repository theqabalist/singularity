machine:
    node:
        version: 0.11.13
    environment:
        CIRCLE_ARTIFACTS: packages
test:
    post:
        - node_modules/.bin/gulp style
        - node_modules/.bin/jasmine-node spec
        - ./package